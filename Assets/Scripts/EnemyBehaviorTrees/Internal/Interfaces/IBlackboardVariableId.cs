namespace EnemyBehaviorTrees.Internal
{
    // this is here if we need it, but it's a hassle to actually use this at the moment when it's a direct wrapper to a string.
    public interface IBlackboardVariableId
    {
        string name { get; }
    }
}